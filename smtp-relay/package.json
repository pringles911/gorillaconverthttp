{
  "name": "smtp-relay",
  "version": "1.0.0",
  "description": "HTTP-to-SMTP relay for sending email from serverless backends (Demo Deploy) where outbound SMTP is blocked.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.3",
    "nodemailer": "^6.9.14",
    "mailparser": "^3.7.2"
  }
}
