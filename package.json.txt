{
  "name": "smtp-relay",
  "version": "1.0.0",
  "description": "HTTP-to-SMTP relay for sending email from serverless backends (Deno Deploy) where outbound SMTP is blocked.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2",
    "nodemailer": "^6.9.14"
  }
}